@using SherpaFrontEnd.Model
<p>Choose name for the team:</p>
<EditForm Model="Team" OnValidSubmit="Add">
    <DataAnnotationsValidator/>
    <ValidationSummary/>

    <InputText @bind-Value="Team.Name"/>

    <div class="btn-group w-100 mt-5" role="group">
        <button class="w-50 btn btn-outline-danger btn-sm" @onclick="Cancel">Cancel</button>
        <button type="submit" class="w-50 btn btn-outline-primary btn-sm">Add</button>
    </div>
</EditForm>

@code {

    [CascadingParameter]
    BlazoredModalInstance? ModalInstance { get; set; }

    private Team Team { get; } = new();

    void Cancel()
    {
        ModalInstance?.CancelAsync();
    }

    private void Add()
    {
        ModalInstance?.CloseAsync(ModalResult.Ok(Team));
    }

}